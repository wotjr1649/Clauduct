# 최초 공개 PR 리뷰 — 2026-09-14

검토 기준은 기존 main `a64bff1`과 로컬 설치 후보 `185a16e`다. 두 후보 사이에는 166커밋, 264파일 변경이 있다. 설치기 두 커밋만의 PR이 아니며, native 라우팅·자식 결과·Workflow 재개·오류와 예산 처리·검증 도구의 누적 변경을 포함한다.

이번 리뷰는 작업을 수행한 Codex의 자체 리뷰다. 독립된 사람이나 별도 에이전트의 승인으로 표시하지 않는다. 전체 파일의 모든 행을 새로 감사했다고 주장하지 않으며 기존 변경별 검증 기록에 현재 핵심 경계의 코드 검토와 회귀를 더했다.

## 발견 및 조치

- README에 개발 머신의 절대 경로와 설치기 도입 전 PATH 설명이 남아 있었다. 설치한 명령과 새 설치 문서로 연결하고, 사용자 PATH 변경과 `-NoPathUpdate`를 설명하도록 수정했다. 사용자 루트의 별도 미커밋 README는 보존했다.
- 게시 전 상태를 단정한 설치 문서는 Release 존재 조건과 해당 Release의 검증 범위를 참조하도록 수정했다. 게시 전후에 같은 문서를 읽을 수 있다.
- 공개 대상 HEAD의 도달 가능한 blob/commit 1,655개, 24,215,628bytes를 로컬에서 검사했다. 개인 키·JWT·주요 provider token·credential 포함 URL·긴 credential literal 패턴 및 인증/설정/세션 원문 파일명에서 일치가 없었다. 이는 정해진 패턴 검사 결과이며 모든 형태의 비밀 부재를 수학적으로 증명하지 않는다. 원본 값은 출력하거나 보고서에 복제하지 않았다. 이력의 일반 Git 작성자 metadata와 프로젝트 개발 기록은 이력 공개 범위에 포함된다.

## 검토한 경계와 관측

- credential supplier는 OS 사용자 홈과 파일 저장소를 확인하고 계정 변경을 거부한다. 설치기의 버전 확인은 로그인 성공 검사가 아니며 인증을 갱신하거나 복사하지 않는다.
- 수집한 자식 결과는 session·parent·request·receipt·metadata를 연결하고 비동기 읽기 뒤 다시 확인한다. 취소와 변경된 요청은 이전 결과 소비를 허용하지 않으며 일괄 소비 구간에 await가 없다.
- 저장 Workflow 재개는 원래 호출과 script digest, 저장 journal과 native 결과를 대조한다. 이미 끝난 worker의 동일 세션 재개라는 기존 실사용 범위를 유지한다. 활성 worker 접근 거부와 복합 복구의 과거 미검증을 성공으로 바꾸지 않는다.
- 설치기는 archive 및 파일 해시·경로·크기·파일 유형을 확인하고 기존 명령과 버전을 보존한다. Node/Claude/Codex 자동 설치, 전역 인증 변경, 설치 잠금 자동 무시는 없다.
- 추가 회귀 14파일이 모두 통과했다. 결과 중계29, 완료 선택46, 합성 credential 복구23, credential 저장소40, 실패 완료 재개23, 생성 전 fixture 예산19, 설치 진단10, runtime 탐색9, runtime 경로7, Workflow 재개46, Workflow 선택36 검사를 포함한다. launcher/native protocol/admission 파일도 통과했다. 실제 모델/사용자 credential 사용은0이다. 일부 검사 내부의 기존 symlink NOT_RUN은 유지한다.
- 설치기의 공개 fixture26검사, 실제 CLI 버전 조회, 새 경로 설치227파일 해시·두 명령 dry-run·재현 ZIP 증거는 `185a16e`의 로컬 릴리즈 기록에 있다. 문서 수정 뒤 실행 코드 동일성을 확인하고 최종 merged commit의 배포본을 다시 검사한다.

현재 검토에서 남은 merge 차단 결함은 발견하지 않았다. 기존 로컬 실사용 승인 범위로 진행하며, 전체 무인 안정성 HOLD/장기 및 복합 복구 한계는 유지한다. PowerShell 5.1 실행 정책 차단, 다른 머신 호환성, 실제 사용자 PATH 쓰기는 미검증이다. merge와 Release는 보호 규칙을 우회하지 않고 진행한다. 게시된 파일의 다운로드 및 SHA 확인은 게시 후 별도 증거로 기록한다.
