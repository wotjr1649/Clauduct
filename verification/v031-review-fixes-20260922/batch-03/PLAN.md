# v0.3.1 수리·검증·리뷰 사이클

2026-09-22. 대상 `D:\AIDEV\clauduct-v031`, `fix/v031`.
기존 변경은 보존한다. 보호 On 유지. 설치·배포·commit·push는 이 사이클에 포함하지 않는다.

1. A1-03 / A3-03 / INT-02: Workflow 선택과 실패 집계를 수정하고 정상·거부 대조군을 검사한다.
2. A1-02 / INT-05 / A1-05: native 인자 경계와 plugin 역할 불확실성을 보완한다.
   A3-01은 선택적 count 분류만 맞춘다. 일반 생성·압축에는 사전 계수를 추가하지 않는다.
3. A5-03 / A5-04 / A5-01: CI 검수기 검사, 취소 누계 대조군, 과거 증거 판본을 보완한다.
   HOLD는 실제 도달 조건을 검사하고 근거로 판정한다.
4. 같은 후보에서 일반·race·태그 검사, 보호 On 실제 native/backend 도구·자식 작업,
   같은 프로세스의 후속 작업과 중복 실행 방지를 검증한 뒤 재리뷰한다.

각 수정은 최소 실패 재현 → 수정 → 통과 → 수정 제거 시 실패를 연결한다.
공개 합성 입력만 실호출하며 예산·시간·프로세스 종료를 제한한다.
검증하지 못한 항목은 NOT_RUN/HOLD로 남기며 제품 PASS로 바꾸지 않는다.

시작 파일 해시는 `before.json`, 복구용 원본은 `.tmp/v031-review-batch3-20260922/before`에 있다.
최종 보고서는 수정/검증/리뷰 결과와 후보 해시를 연결한다.
